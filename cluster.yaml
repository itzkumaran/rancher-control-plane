nodes:
    user: "ubuntu" 
    user: "ubuntu" 
    user: "ubuntu"
    role: [controlplane]
services:
  etcd:
    snapshot: "true" 
    creation: "6h" 
    retention: "24h" 

ignore_docker_version: "true" 

